# Alexandria Frontend — Environment Variables
# In development: leave VITE_API_URL empty (Vite proxy handles it)
# In production (Vercel): set VITE_API_URL to your Render backend URL

VITE_API_URL=
VITE_DEFAULT_LANGUAGE=uk
